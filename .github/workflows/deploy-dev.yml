name: Deploy to Dev
on:
  push:
    branches-ignore:
      - main

jobs:
  deploy:
    uses: seamuslowry/workflows/.github/workflows/deploy_python_functions.yml@feature/deploy_python_function
    with:
      name: hundredandten
    secrets:
      azure_publish_profile: ${{ secrets.AZURE_DEV_PUBLISH }}
