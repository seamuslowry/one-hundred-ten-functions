name: Deploy to Dev
on:
  push:
    branches-ignore:
      - main

jobs:
  deploy:
    environment: dev
    uses: seamuslowry/hundred-and-ten-serverless/.github/workflows/deploy.yml@feature/ci_deploy
    with:
      name: hundredandten
      environment: dev
